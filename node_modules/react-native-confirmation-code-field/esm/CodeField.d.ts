import {
  StyleProp,
  TextInput,
  TextInputProps,
  TextStyle,
  ViewProps,
} from 'react-native';
import React, {ComponentType, ReactNode} from 'react';
export interface RenderCellOptions {
  symbol: string;
  index: number;
  isFocused: boolean;
}
declare type TextInputPropsWithoutStyle = Omit<TextInputProps, 'style'>;
export interface Props extends TextInputPropsWithoutStyle {
  renderCell: (options: RenderCellOptions) => ReactNode;
  RootProps?: ViewProps;
  RootComponent?: ComponentType<ViewProps>;
  rootStyle?: ViewProps['style'];
  textInputStyle?: StyleProp<TextStyle>;
  cellCount?: number;
}
export declare const CodeField: React.ForwardRefExoticComponent<
  Props & React.RefAttributes<TextInput>
>;
export {};
